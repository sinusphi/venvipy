language: python
os: linux
dist: xenial

python:
  - "3.7"
  - "3.8"

before_script:
  - pip install --upgrade pip
  - pip install wheel

install:
  - pip install -r requirements.txt
  - pip install --upgrade venvipy

script:
  - venvipy
